#ifndef _GENHEADFIL_H
#define _GENHEADFIL_H

#define SUCCESS   1

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <ctype.h>


#define max(X, Y)  (((X) > (Y)) ? (X) : (Y))
#define min(X, Y)  (((X) < (Y)) ? (X) : (Y))

#endif