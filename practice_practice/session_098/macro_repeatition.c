#define SOME_MACRO(a, b)   ((a) > (b) ? (a) : (b))

#define SOME_MACRO(a, b)    ((a) > (b) ? (a) : (b))

#define SOME_MACRO(a, b)   ((a) > (b) ? (a) : (b))


#define SOME_MACRO(a, b)   ((a) > (b) ? (a) : (b))

#define SOME_MACRO(a, b)    ((a) > (b) ? (a) : (b))

#define SOME_MACRO(a, b)   ((a) > (b) ? (a) : (b))

