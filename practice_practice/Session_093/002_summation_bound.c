#include <stdio.h>
#include <stdlib.h>

int input_arr(int* p_arr, int size);
int output_arr(int* p_arr, int size);
int find_pair();


int main(void)
{
    
    int nr_of_ele, size_of_ele;

    printf("Enter the number and size of elements:\n");
    scanf("%d%d", nr_of_ele, size_of_ele);
    int* p_arr = (int*)calloc(nr_of_ele, size_of_ele);

}


int input_arr(int* p_arr, int size)
{

}
